% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power_events_rate.R
\name{power_events_rate}
\alias{power_events_rate}
\title{Probability of Observing At Least a Given Number of Events}
\usage{
power_events_rate(n, r, e)
}
\arguments{
\item{n}{Integer or vector of integers. Sample size(s).}

\item{r}{Numeric or vector of numerics. Risk(s) (per-subject event
probability), between 0 and 1.}

\item{e}{Integer or vector of integers. Event count threshold(s), e.g. 1, 2, 3.}
}
\value{
A matrix of class \code{power_events_rate} with columns:
\describe{
\item{N}{Sample size}
\item{Risk}{Per-subject event probability}
\item{>= e}{One column per threshold in \code{e}, named using the
"greater than or equal to" symbol followed by the threshold value,
giving P(X >= e) for X ~ Binomial(N, Risk)}
}
}
\description{
Computes the exact binomial probability of observing at least \code{e}
events, for every combination of sample size and risk, and for one or more
event thresholds.
}
\examples{
power_events_rate(30, 0.1, 1:3)
power_events_rate(c(30, 60), c(0.05, 0.1), c(1, 2, 3))

}
